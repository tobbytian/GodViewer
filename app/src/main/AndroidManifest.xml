<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Resolve mirrored app labels/icons on Android 11+ -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>

    <application
        android:name=".GodViewerApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="xposedmodule"
            android:value="true" />
        <meta-data
            android:name="xposeddescription"
            android:value="Debug your app, change view attributes." />
        <meta-data
            android:name="xposedminversion"
            android:value="53" />
        <!--
          Desktop icon is optional (LauncherAlias).
          MODULE_SETTINGS lets LSPosed open this UI from the module page
          even when the launcher alias is disabled.
        -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="de.robv.android.xposed.category.MODULE_SETTINGS" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.APPLICATION_PREFERENCES" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity-alias
            android:name=".LauncherAlias"
            android:enabled="true"
            android:exported="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:targetActivity=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>

        <activity
            android:name=".ui.host.SimpleTextActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".ui.host.DonateActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity" />


        <activity
            android:name=".RuleMirrorListActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".RuleMirrorDetailActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".RuleMirrorRuleDetailActivity"
            android:exported="false"
            android:parentActivityName=".RuleMirrorDetailActivity" />

        <!-- Explicit component from target processes; exported so other UIDs can deliver. -->
        <receiver
            android:name=".data.RuleMirrorReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="com.godviewer.app.ACTION_MIRROR_RULES" />
            </intent-filter>
        </receiver>

        <!-- Host control: target foreground reports + notification action buttons. -->
        <receiver
            android:name=".data.HostControlReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="com.godviewer.app.ACTION_TARGET_FOREGROUND" />
                <action android:name="com.godviewer.app.action.HOST_ENABLE_EDIT" />
                <action android:name="com.godviewer.app.action.HOST_UNDO" />
                <action android:name="com.godviewer.app.action.HOST_MANAGE_RULES" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
