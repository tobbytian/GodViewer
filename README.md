# 👁 GodViewer 上帝视角

- 一款基于 **Xposed / LSPosed** 的「上帝视角」运行时视图调试工具。
- 在**Android16和Lsposed API102**测试通过
- 不用重启应用、不用系统作用域，直接在目标应用上**点一点就能把它的 UI 改成你想要的样子**，并且具有**持久性**。

## ✨ 能干什么

- 👆 **点哪改哪**：触摸屏幕直接选中视图，比 F12 还顺滑
- 🎛️ **属性随便动**：尺寸 / 边距 / 内边距 / 可见性、`TextView` 文本与最大行数、`ImageView` 图片 URL 与缩放
- 🙈 **隐藏 & 重置**：一键隐藏视图；改过的视图随时恢复原样
- 💾 **改了就不丢**：修改存成规则，目标应用重启 / 杀进程 / 重启手机后自动重放
- 🔕 **无感使用**：编辑模式默认关闭，通知栏一键「开启」；退出后不留任何 UI，想怎么用怎么用

## 使用方法

1. **LSPosed** 勾选目标应用并重启
2. 下拉通知栏，点「编辑模式」通知上的「**开启**」（需要目标应用有通知权限）
3. 点一下想改的控件 → 改 → **应用**，完事 或 直接**隐藏**
4. 编辑完在弹窗点「**退出编辑模式**」→ 恢复正常使用，干净利落

## 工作原理

模块注入目标应用进程，点击被拦截后弹出编辑弹窗；修改以 JSON 规则存在**目标应用自己的数据目录**，重启自动重放。不注入系统服务、不需要系统级权限，只在你勾选的目标应用里活动。

## 🛠 构建

```bash
./gradlew assembleRelease
```

产物：`app/build/outputs/apk/release/app-release.apk`（需 JDK 17 + Android SDK platform 35）

## 许可

[GPL-3.0](LICENSE)

---

> 本软件完全免费且开源，仅供学习交流使用。请从本仓库获取源码自行构建，避免来路不明的安装包。

## 未实现的功能（bug）
- 1.规则管理功能（~~是的目前的隐藏是无法被撤回的~~ ✅ v2.2.0 已解决：支持撤销上一个操作 + 规则管理弹窗可查看/删除任意规则，隐藏的视图删除后即时恢复）
- 2.对于过大的父控件导致的编辑键不可见的问题
